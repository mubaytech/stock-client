import { QrScannerDirective } from './qr-scanner.directive';

describe('QrScannerDirective', () => {
  it('should create an instance', () => {
    const directive = new QrScannerDirective();
    expect(directive).toBeTruthy();
  });
});
