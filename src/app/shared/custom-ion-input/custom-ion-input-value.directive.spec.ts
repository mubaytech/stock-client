import { CustomIonInputValueDirective } from './custom-ion-input-value.directive';

describe('CustomIonInputValueDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomIonInputValueDirective();
    expect(directive).toBeTruthy();
  });
});
