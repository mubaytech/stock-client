import { CustomIonInputPlaceholderDirective } from './custom-ion-input-placeholder.directive';

describe('CustomIonInputPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomIonInputPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
