import { CustomIonInputHeaderDirective } from './custom-ion-input-header.directive';

describe('CustomIonInputHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomIonInputHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
