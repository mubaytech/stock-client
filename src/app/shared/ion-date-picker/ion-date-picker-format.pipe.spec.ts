import { IonDatePickerFormatPipe } from './ion-date-picker-format.pipe';

describe('IonDatePickerFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new IonDatePickerFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
