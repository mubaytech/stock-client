import { ScrollbarThemeDirective } from './scrollbar-theme.directive';

describe('ScrollbarThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollbarThemeDirective();
    expect(directive).toBeTruthy();
  });
});
