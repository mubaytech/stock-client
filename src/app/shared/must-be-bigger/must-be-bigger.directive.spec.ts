import { MustBeBiggerDirective } from './must-be-bigger.directive';

describe('MustBeBiggerDirective', () => {
  it('should create an instance', () => {
    const directive = new MustBeBiggerDirective();
    expect(directive).toBeTruthy();
  });
});
