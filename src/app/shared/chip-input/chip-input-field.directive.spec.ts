import { ChipInputFieldDirective } from './chip-input-field.directive';

describe('ChipInputFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipInputFieldDirective();
    expect(directive).toBeTruthy();
  });
});
