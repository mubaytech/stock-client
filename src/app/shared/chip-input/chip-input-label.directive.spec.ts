import { ChipInputLabelDirective } from './chip-input-label.directive';

describe('ChipInputLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipInputLabelDirective();
    expect(directive).toBeTruthy();
  });
});
