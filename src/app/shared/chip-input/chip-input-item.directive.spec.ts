import { ChipInputItemDirective } from './chip-input-item.directive';

describe('ChipInputItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipInputItemDirective();
    expect(directive).toBeTruthy();
  });
});
