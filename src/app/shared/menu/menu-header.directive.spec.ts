import { MenuHeaderDirective } from './menu-header.directive';

describe('MenuHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
