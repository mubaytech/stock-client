import { MenuFooterDirective } from './menu-footer.directive';

describe('MenuFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuFooterDirective();
    expect(directive).toBeTruthy();
  });
});
