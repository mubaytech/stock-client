import { IonicSelectableSearchbarTemplateDirective } from './ionic-selectable-searchbar-template.directive';

describe('IonicSelectableSearchbarTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new IonicSelectableSearchbarTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
