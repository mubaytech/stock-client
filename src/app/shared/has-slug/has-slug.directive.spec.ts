import { HasSlugDirective } from './has-slug.directive';

describe('HasSlugDirective', () => {
  it('should create an instance', () => {
    const directive = new HasSlugDirective();
    expect(directive).toBeTruthy();
  });
});
