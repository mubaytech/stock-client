import { MaterialIconsDirective } from './material-icons.directive';

describe('MaterialIconsDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialIconsDirective();
    expect(directive).toBeTruthy();
  });
});
