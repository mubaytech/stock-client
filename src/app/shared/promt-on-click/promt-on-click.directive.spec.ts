import { PromtOnClickDirective } from './promt-on-click.directive';

describe('PromtOnClickDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
      const directive = new PromtOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
