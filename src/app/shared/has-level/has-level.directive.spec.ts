import { HasLevelDirective } from './has-level.directive';

describe('HasLevelDirective', () => {
  it('should create an instance', () => {
    const directive = new HasLevelDirective();
    expect(directive).toBeTruthy();
  });
});
