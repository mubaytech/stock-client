import { MatPaginationIntl } from './mat-pagination-intl';

describe('MatPaginationIntl', () => {
  it('should create an instance', () => {
    expect(new MatPaginationIntl()).toBeTruthy();
  });
});
